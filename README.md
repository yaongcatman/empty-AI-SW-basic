# empty-AI-SW-basic
# 📋 시스템 관제 자동화 프로젝트 수행 내역서
24시간 쉬지 않는 인공지능 경비원 설치 과정
---

## 1. 설정 및 명령어 기록 (Implementation Log)

### 🔐 1.1 SSH 및 방화벽 설정
*   **SSH 포트 변경 및 Root 접속 차단**
*   도둑이 들어오지 못하게 집을 보강하고 담장을 쌓는 단계
    ```bash
    # /etc/ssh/sshd_config 수정 내역
    Port 20022
    PermitRootLogin no
    
    # 서비스 재시작
    sudo systemctl restart ssh
    ```
    * 뒷문 만들기 (SSH 포트 변경): 도둑들이 주로 노리는 정문(22번) 대신, 나만 아는 뒷문(20022번)을 새로 만듭니다.

    * 집주인 보호 (Root 접속 차단): 모든 권한을 가진 집주인 계정으로 직접 들어오는 것을 막아, 비밀번호 하나로 집 전체가 털리는 것을 방지합니다.
    
*   **방화벽(UFW) 설정**
    ```bash
    sudo ufw allow 20022/tcp
    sudo ufw allow 15034/tcp
    sudo ufw enable
    ```
    * 마당 검문소 (방화벽 UFW): 집 주변에 담장을 치고, 허락된 포트(20022, 15034)만 통과시키는 검문소를 세웁니다.
  

### 👥 1.2 계정 및 권한 체계 구축
*   **계정 및 그룹 생성**
*   가족마다 들어갈 수 있는 방을 나누어 피해를 최소화하는 단계 
    ```bash
    # 그룹 생성
    sudo groupadd agent-common
    sudo groupadd agent-core

    # 계정 생성 및 그룹 할당
    sudo useradd -m -G agent-core,agent-common agent-admin
    sudo useradd -m -G agent-core,agent-common agent-dev
    sudo useradd -m -G agent-common agent-test
     ```
    * 계정 나누기: 관리자, 개발자, 테스터에게 각자 다른 아이디를 줍니다.  
    
    *   **디렉토리 권한 및 ACL 설정**
    ```bash
    # AGENT_HOME 설정 및 권한 부여
    sudo chown :agent-common $AGENT_HOME/upload_files
    sudo chmod 770 $AGENT_HOME/upload_files
    
    # 보안 디렉토리 및 로그 디렉토리 (agent-core 전용)
    sudo chown :agent-core $AGENT_HOME/api_keys /var/log/agent-app
    sudo chmod 770 $AGENT_HOME/api_keys /var/log/agent-app
    ```
    *방마다 자물쇠 채우기: 테스터는 테스트 방만, 관리자는 금고 방까지 들어갈 수 있게 설정합니다. 이렇게 하면 누군가 실수해도 다른      방은 안전합니다.
    
### ⚙️ 1.3 환경 변수 및 자동화 등록
*   **환경 변수 설정 (`.bashrc` 또는 `/etc/environment`)**
*   앱을 실행하기 전, 주변이 완벽한지 검사하는 부트 시퀀스(Boot Sequence) 과정
       ```bash
    export AGENT_HOME="/home/agent-admin/agent-app"
    export AGENT_PORT=15034
    ```
    * 이정표 세우기 (환경 변수): 앱이 길을 잃지 않게 "우리 집 주소는 여기야"라고 미리 장소를 알려줍니다.
    * 5단계 합격: 계정 권한이나 필수 파일이 있는지 하나씩 체크해서 모두 [OK]가 떠야 "준비 완료(Ready)" 상태가 됩니
*   **Crontab 등록 (agent-admin 계정)**
    ```bash
    # crontab -e
    * * * * * /home/agent-admin/agent-app/bin/monitor.sh
    ```
* 로봇이 멈추지 않고 일하게 하고, 관찰한 기록을 관리하는 마지막 단계.
* 알람 시계 (Crontab): 리눅스의 알람 시계를 맞춰 1분마다 로봇(monitor.sh)을 깨워 감시하게 시킵니다.
* 시스템 일기장 (로그): 로봇이 본 내용을 일기(monitor.log)로 남깁니다. 나중에 문제가 생기면 이 일기를 보고 원인을 찾습니다.
* 일기장 정리: 일기가 너무 많아져 창고를 차지하지 않게, 오래된 일기는 압축하거나 버리는 규칙을 세웁니다.
---

## 2. 필수 증거 자료 체크리스트 (Evidence Checklist)


### ⁂SSH 포트(20022) 및 Root 차단   
코디
```bash
# 포트 20022로 변경 및 Root 접속 차단
sudo sed -i 's/^#\?Port .*/Port 20022/' /etc/ssh/sshd_config
sudo sed -i 's/^#\?PermitRootLogin .*/PermitRootLogin no/' /etc/ssh/sshd_config

# 설정 적용 (ssh 서비스 재시작)
sudo systemctl restart ssh

# 설정 적용 확인
ss -tuln | grep 20022
```
## 📖 SSH와 Root: 서버 관리의 기초 개념

서버라는 '디지털 요새'를 관리하기 위해 반드시 이해해야 하는 두 가지 핵심 요소입니다.

---

### 1. SSH (Secure Shell) - "안전한 원격 통로"

| 항목 | 상세 설명 |
| :--- | :--- |
| **의미** | **S**ecure(보안) **Sh**ell(명령어 창)의 약자로, 네트워크를 통해 다른 컴퓨터에 안전하게 접속하기 위한 프로토콜입니다. |
| **왜 필요한가?** | 예전의 접속 방식은 데이터가 평문으로 전송되어 해킹에 취약했습니다. SSH는 모든 데이터를 **암호화**하여 보안을 유지합니다. |
| **언제 사용하는가?** | 원격지에 있는 서버를 내 컴퓨터에서 제어하고 싶을 때(파일 전송, 설정 변경 등) 언제나 사용합니다. |
| **어디서 작동하나?** | 서버의 네트워크 입구(기본 22번 포트)에서 대기하며 외부 사용자의 신호를 기다립니다. |
| **어떻게 기능하나?** | 공개키/개인키 암호화 방식을 사용하여 접속하려는 사람이 본인이 맞는지 확인하고, 연결된 후의 모든 대화를 암호화합니다. |



---

### 2. Root (루트) - "절대 권한의 뿌리"

| 항목 | 상세 설명 |
| :--- | :--- |
| **의미** | 시스템의 최상위 계정으로, 나무의 **'뿌리'**처럼 모든 권한이 시작되는 리눅스의 절대 관리자 계정(Superuser)입니다. |
| **왜 필요한가?** | 일반 사용자는 시스템 설정이나 중요한 파일을 건드릴 수 없습니다. 시스템 전체를 관리하거나 보안을 설정하려면 반드시 Root 권한이 필요합니다. |
| **언제 사용하는가?** | 프로그램 설치, 사용자 관리, 시스템 업데이트, 보안 설정 등 **관리자 전용 업무**를 수행할 때만 잠시 사용합니다. |
| **어디서 작동하나?** | 리눅스 시스템 전체에서 통용되며, 파일 시스템의 최상위 경로(`/`)부터 모든 자원에 접근할 수 있습니다. |
| **어떻게 기능하나?** | 모든 보안 검사를 통과하며, 어떤 파일이든 읽고 쓰고 실행할 수 있는 무소불위의 권한을 행사합니다. |



---

### 3. 왜 이 둘의 보안이 그토록 중요한가요?

해커에게 **"SSH를 뚫고 Root 권한을 얻는다"**는 것은 서버의 심장을 완전히 장악했다는 뜻입니다.

1.  **공격의 관문 (SSH)**: 해커가 서버 내부로 들어오는 유일한 길입니다. 이 길이 뚫리면 우리 집 안방까지 해커가 들어오게 됩니다.
2.  **공격의 목표 (Root)**: 해커가 가장 탐내는 보물입니다. Root 권한만 얻으면 서버 내의 모든 정보를 빼가거나 서버를 완전히 파괴할 수 있습니다.

---

### 💡 요약: 왜 보안 설정을 하나요?
* **SSH 포트 변경**: 해커가 우리 집으로 들어오는 **'통로' 자체를 못 찾게** 하기 위해 (비밀 통로 만들기)
* **Root 접속 차단**: 설령 해커가 통로를 찾았더라도, **'가장 강력한 열쇠(Root)'**는 외부인이 절대 만질 수 없게 안방 금고에 숨겨두기 위해 (보안 강화)

## 🔐 SSH 보안 강화 (포트 변경 및 Root 접속 차단)

서버의 기본 출입문인 SSH 설정을 변경하여 해커의 공격 경로를 차단하고 관리자 권한을 보호하는 핵심 보안 작업입니다.

---

### 🧐 왜 SSH 설정을 변경해야 하나요?

서버가 인터넷에 연결되는 순간, 전 세계의 해킹 봇들이 **'22번 포트'**와 **'root'** 계정을 타겟으로 공격을 시작합니다. 
* **포트 변경**: 모두가 아는 정문을 닫고 나만 아는 **비밀 통로**를 만듭니다.
* **Root 차단**: 집의 모든 권한을 가진 **마스터 키(Root)**가 외부로 노출되지 않도록 원천 봉쇄합니다.

---

### 📝 명령어 한 줄 해석 (언제, 어디서, 어떻게, 왜)

1. `sudo sed -i 's/^#\?Port .*/Port 20022/' /etc/ssh/sshd_config`
   * **언제/어디서**: 서버 초기 세팅 시, SSH 설정 파일 내에서 접속 번호를 바꿀 때 사용합니다.
   * **어떻게/왜**: 기본 22번 포트를 20022번으로 수정하여 자동화된 해킹 공격을 피합니다.
   * **명령어 구조**: `sed -i 's/기존포트패턴/새포트설정/' 파일경로`

2. `sudo sed -i 's/^#\?PermitRootLogin .*/PermitRootLogin no/' /etc/ssh/sshd_config`
   * **언제/어디서**: 포트 변경과 동시에, 관리자 계정의 직접 노출을 막기 위해 실행합니다.
   * **어떻게/왜**: 외부에서 Root 계정으로 바로 로그인하는 것을 금지(no)하여 보안 사고를 예방합니다.
   * **명령어 구조**: `sed -i 's/기존루트로그인패턴/루트로그인금지설정/' 파일경로`

3. `sudo systemctl restart ssh`
   * **언제/어디서**: 설정 파일의 텍스트를 수정한 직후, 시스템에 반영하기 위해 사용합니다.
   * **어떻게/왜**: SSH 서비스를 재시작하여 경비원이 바뀐 보안 규칙(20022번 포트 사용)을 읽게 합니다.
   * **명령어 구조**: `systemctl [명령] [서비스명]`

4. `ss -tuln | grep 20022`
   * **언제/어디서**: 모든 설정을 마친 후, 실제로 문이 잘 열렸는지 확인할 때 사용합니다.
   * **어떻게/왜**: 20022번 포트가 현재 '대기(Listen)' 상태인지 내 눈으로 직접 검증합니다.
   * **명령어 구조**: `네트워크상태확인 | 특정글자찾기`

---

### 🔍 명령어 상세 분석 (약자 및 의미)

| 명령어/옵션 | 약자 및 뜻 | 사용법 및 왜 사용하는가? | 결과 |
| :--- | :--- | :--- | :--- |
| **`sed`** | **S**tream **Ed**itor | 파일을 열지 않고 텍스트를 자동으로 수정할 때 사용 | 설정 파일 내용 변경 |
| **`-i`** | **i**n-place | 수정 내용을 파일에 즉시 저장하기 위해 사용 | 파일 덮어쓰기 완료 |
| **`Port`** | 포트 (통로) | 데이터가 드나드는 문 번호를 지정함 | 접속 통로 번호 확정 |
| **`RootLogin`** | 루트 로그인 | 최고 관리자 계정으로의 접속 허용 여부 | 관리자 접근 권한 설정 |
| **`systemctl`** | **system** **c**on**t**rol | 시스템 서비스(SSH 등)를 제어하고 관리함 | 서비스 상태 변경 권한 |
| **`restart`** | 재시작 | 변경된 설정값을 실제 시스템에 적용하기 위해 사용 | 서비스 초기화 및 재가동 |
| **`ss`** | **s**ocket **s**tatistics | 현재 컴퓨터의 네트워크 연결 통계를 보여줌 | 열린 포트 목록 출력 |
| **`grep`** | **G.R.E.P** (찾기) | 방대한 데이터 중 내가 원하는 글자만 필터링할 때 사용 | 20022 포트 존재 확인 |

---

### 🧩 `sed` 명령어 속 정규 표현식 해부하기

명령어에 포함된 `'s/^#\?PermitRootLogin .*/PermitRootLogin no/'`의 의미를 쪼개어 분석합니다.

#### 1. `s/` : 찾아서 바꾸기 (Substitute)
* **의미**: "지금부터 **S**ubstitute(교체) 작업을 시작할게!"라는 신호입니다.
* **구조**: `s/찾을내용/바꿀내용/` 형식으로 사용됩니다.

#### 2. `^#\?` : 주석 처리된 줄까지 모두 찾기
* **`^` (Caret)**: "줄의 맨 처음부터 시작하는 것만 찾아라"라는 뜻입니다.
* **`#`**: 리눅스 설정 파일에서 `#`은 주석(설명 처리되어 작동 안 함)을 의미합니다.
* **`\?`**: 바로 앞의 글자(`#`)가 **"있을 수도 있고, 없을 수도 있다"**는 뜻입니다.
    * **이유**: 설정이 `#PermitRootLogin`으로 되어 있든, 그냥 `PermitRootLogin`으로 되어 있든 상관없이 다 찾아내기 위해서입니다.

#### 3. `.*` : 그 뒤에 뭐가 있든 끝까지 선택
* **`.` (Dot)**: "아무 글자나 한 글자"를 의미합니다.
* **`*` (Asterisk)**: "그게 몇 개가 있든 상관없다(0개 이상)"는 뜻입니다.
* **이유**: `PermitRootLogin` 뒤에 기존에 어떤 설정값이 있든 상관없이 **그 줄 전체를 싹 다 잡아서 지우기 위해** 사용합니다.

#### 4. `/PermitRootLogin no/` : 새로운 값으로 덮어쓰기
* **의미**: 위에서 찾은 지저분한 줄 전체를 지우고, 그 자리에 아주 깔끔하게 `PermitRootLogin no`라는 글자를 딱 써 넣으라는 명령입니다.

> **💡 정규식 한 줄 요약**
> "주석(`#`)이 달려 있든 아니든, `PermitRootLogin`으로 시작하는 줄을 통째로 찾아서, 깨끗하게 `PermitRootLogin no`로 바꿔버려!"

---

### 💡 최종 요약
이 작업은 **"모두가 아는 22번 대문을 폐쇄하고 20022번 비밀 쪽문을 만든 뒤, 집주인(Root)은 정면으로 들어오지 못하게 못 박아 보안을 2중으로 강화하는 과정"**입니다.

<img width="566" height="108" alt="스크린샷 2026-05-14 오후 10 22 22" src="https://github.com/user-attachments/assets/1de527ba-714d-4331-8177-602ef910c073" />
  
설정을 모두 마친 후, 시스템이 새로운 보안 규칙에 따라 정상적으로 작동하고 있는지 확인한 결과 데이터입니다.

#### 1. 결과값 한 줄 해석

| 결과 데이터 (출력값) | 한 줄 해석 |
| :--- | :--- |
| **`tcp`** | 데이터의 신뢰성을 보장하는 **TCP 통신 방식**을 사용하고 있습니다. |
| **`LISTEN`** | 서버가 현재 해당 포트에서 접속 신호를 **'경청(대기)'**하고 있는 아주 건강한 상태입니다. |
| **`0` / `128`** | 현재 대기열(Queue) 상태를 나타내며, 접속 요청을 받을 준비가 충분히 되어 있음을 뜻합니다. |
| **`0.0.0.0:20022`** | 구형 주소 체계(IPv4)를 사용하는 **모든 접속자**가 20022번 문으로 들어올 수 있게 열려 있습니다. |
| **`[::]:20022`** | 신형 주소 체계(IPv6)를 사용하는 **모든 접속자**를 위해 20022번 문을 열어두었습니다. |
| **`0.0.0.0:*` / `[::]:*`** | 외부의 어떤 IP 주소나 어떤 포트에서 접속을 시도하든 상관없이 받아들이겠다는 뜻입니다. |

---

### 🔍 항목별 상세 의미 (구조적 분석)

* **`Netid (tcp)`**: 네트워크 식별자입니다. SSH는 연결 지향형인 TCP를 사용합니다.
* **`State (LISTEN)`**: 가장 핵심적인 부분입니다. 서비스가 죽어있지 않고 **'살아서 대기 중'**임을 증명합니다.
* **`Local Address (0.0.0.0:20022)`**: 우리 서버의 주소와 문 번호입니다. `20022`가 표시됨으로써 **포트 변경 설정이 완벽히 적용**되었음을 알 수 있습니다.
* **`Peer Address (*:*)`**: 우리 서버에 접속할 상대방의 주소입니다. 아직 접속자가 없으므로 비어있는 상태(`*`)를 나타냅니다.

---

### 💡 최종 결론
이 데이터는 **"기본 정문(22번)을 닫고, 우리가 설계한 비밀 통로(20022번)를 통해 전 세계 어디에서든 주인이 접속할 수 있도록 경비원(SSH 서비스)이 24시간 감시 체제에 돌입했다"**는 최종 합격 통지서와 같습니다.

##

### ⁂방화벽 허용 (20022, 15034)  
`sudo ufw status verbose`   
```bash
# 방화벽 도구 UFW 설치
sudo apt update
sudo apt install ufw -y

# 포트 허용
sudo ufw allow 20022/tcp
sudo ufw allow 15034/tcp

# 방화벽 활성화
sudo ufw --force enable

# 상태 확인
sudo ufw status

```
## 🛡️ 방화벽(Firewall)의 이해

### 1. 방화벽의 정의와 의미
* **사전적 의미**: 건물에 화재가 발생했을 때 불길이 번지지 않도록 막는 **'방화벽'**에서 유래되었습니다.
* **디지털적 의미**: 외부에서 들어오는 네트워크 트래픽(데이터)과 내부에서 나가는 트래픽을 감시하고, **허가되지 않은 접근을 차단**하는 보안 시스템입니다.

### 2. 방화벽은 왜 필요한가요? (Why)
* **무단 침입 방지**: 서버의 모든 문(Port)을 열어두는 것은 대문을 열어놓고 외출하는 것과 같습니다. 방화벽은 허가된 사람만 들어오게 합니다.
* **불필요한 노출 차단**: 서버 내부에서 돌아가는 서비스 중 외부로 보여줄 필요가 없는 통로들을 숨겨서 해커의 타겟이 되지 않도록 보호합니다.
* **안전한 통신 보장**: 신뢰할 수 있는 IP 주소나 특정 통신 방식(TCP/UDP)만 골라서 허용할 수 있습니다.

### 3. 언제, 어디서, 어떻게 사용하나요?
* **언제 (When)**: 서버를 인터넷에 연결하는 순간부터 **24시간 내내** 가동되어야 합니다. 특히 새로운 서비스를 설치하거나 포트 번호를 바꿨을 때 설정을 변경합니다.
* **어디서 (Where)**: 서버의 **네트워크 입구**에서 작동합니다. 외부 세계와 우리 서버가 만나는 접점에서 모든 데이터를 검사합니다.
* **어떻게 (How)**: 
    1. 기본 정책을 **'모든 접속 차단(Deny)'**으로 설정합니다.
    2. 그중 우리가 꼭 사용해야 하는 통로(예: 20022, 15034)만 **'허용(Allow)'** 목록에 추가합니다.
    3. 이렇게 작성된 **'화이트리스트(허용 명부)'**를 기준으로 데이터를 필터링합니다.

### 4. 주요 기능 (Function)
1. **패킷 필터링**: 들어오고 나가는 데이터(패킷)의 주소와 포트 번호를 확인하여 통과시킬지 결정합니다.
2. **접근 제어**: 특정 IP 주소나 국가에서의 접속을 막거나 허용합니다.
3. **로깅(Logging)**: 누가 언제 우리 서버의 문을 두드렸는지 기록을 남겨, 나중에 침입 흔적을 분석할 수 있게 합니다.

---

### 💡 비유로 이해하기
> 방화벽은 클럽 입구를 지키는 **'보안 요원(가드)'**과 같습니다.
> 
> * **클럽 내부**: 우리 서버
> * **입구**: 포트(Port)
> * **출입 명부**: 방화벽 규칙(UFW Rule)
> 
> 보안 요원은 명부에 이름이 있는 사람(허용된 포트)만 들여보내고, 명단에 없거나 수상해 보이는 사람은 입구에서 즉시 돌려보냅니다.

## 🧱 방화벽(UFW) 설정 및 포트 허용 가이드

서버 보안의 핵심인 방화벽을 설치하고, 우리가 사용할 전용 통로(20022, 15034)를 안전하게 개방하는 과정입니다.

---

### 🧐 왜 방화벽 포트를 허용해야 하나요?

방화벽은 서버의 **'출입 통제소'**입니다. 
기본적으로 방화벽은 모든 문을 꽉 잠가두는데, 우리가 변경한 **SSH 포트(20022)**나 **서비스용 포트(15034)**를 방화벽에서 허용(`allow`)해주지 않으면, 우리 자신도 서버에 접속할 수 없게 됩니다. 즉, **"안전하게 검증된 문만 열어주기 위해"** 사용합니다.

---

### 📝 방화벽(UFW) 설정 명령어 상세 해석

각 명령어가 어떤 구조로 이루어져 있고, 언제 왜 사용하는지 정리한 내역입니다.

#### 1. `sudo ufw status verbose`
* **사용 구조**: `ufw status [옵션]`
* **언제/어디서**: 작업을 시작하기 전 터미널에서 현재 방화벽 상태를 확인할 때 사용합니다.
* **어떻게/왜**: 현재 어떤 문이 열려 있는지 상세히(verbose) 파악하여 중복 설정을 방지합니다.

#### 2. `sudo apt update && sudo apt install ufw -y`
* **사용 구조**: `apt [명령] [패키지명] [옵션]`
* **언제/어디서**: 서버에 방화벽 프로그램(UFW)이 없을 때 설치하기 위해 사용합니다.
* **어떻게/왜**: 최신 패키지 목록을 불러오고(`update`), 자동으로 '예'를 선택(`-y`)하여 UFW를 설치합니다.

#### 3. `sudo ufw allow 20022/tcp`
* **사용 구조**: `ufw allow [포트번호]/[프로토콜]`
* **언제/어디서**: SSH 포트를 20022로 변경한 후, 이 문을 통과시켜야 할 때 사용합니다.
* **어떻게/왜**: 20022번 통로를 TCP 프로토콜(신뢰성 있는 연결)로 열어 원격 접속을 허용합니다.

#### 4. `sudo ufw allow 15034/tcp`
* **사용 구조**: `ufw allow [포트번호]/[프로토콜]`
* **언제/어디서**: 특정 서비스(애플리케이션)가 15034 포트를 사용할 때 실행합니다.
* **어떻게/왜**: 서비스 운영을 위해 해당 포트로 들어오는 신호가 차단되지 않도록 통로를 개방합니다.

#### 5. `sudo ufw --force enable`
* **사용 구조**: `ufw [옵션] enable`
* **언제/어디서**: 모든 포트 설정을 마친 후, 방화벽을 실제 가동할 때 사용합니다.
* **어떻게/왜**: 사용자에게 다시 묻지 않고(`--force`) 즉시 방화벽을 활성화하여 모든 보안 규칙을 적용합니다.

#### 6. `sudo ufw status`
* **사용 구조**: `ufw status`
* **언제/어디서**: 모든 작업이 끝난 후 최종적으로 상태를 점검할 때 사용합니다.
* **어떻게/왜**: 내가 허용(`ALLOW`)한 포트들이 목록에 정상적으로 등록되었는지 내 눈으로 직접 확인합니다.

---

### 🔍 명령어 상세 분석 (약자 및 의미)

| 명령어/옵션 | 약자 및 뜻 | 사용법 및 왜 사용하는가? | 결과 |
| :--- | :--- | :--- | :--- |
| **`ufw`** | **U**ncomplicated **F**ire**w**all | 복잡한 방화벽 설정을 쉽게 하기 위해 사용 | 방화벽 제어 권한 획득 |
| **`apt`** | **A**dvanced **P**ackage **T**ool | 리눅스에서 프로그램을 설치/삭제할 때 사용 | 프로그램 설치 관리 |
| **`status`** | 상태 (Status) | 현재 방화벽이 켜져 있는지, 어떤 포트가 열렸는지 확인 | 포트 리스트 출력 |
| **`allow`** | 허용하다 (Allow) | 특정 포트로 들어오는 데이터를 통과시키기 위해 사용 | 해당 포트 개방 |
| **`tcp`** | **T**ransmission **C**ontrol **P**rotocol | 데이터 전송 시 '신뢰성'을 보장하는 통신 방식 지정 | 안정적인 연결 설정 |
| **`enable`** | 활성화 (Enable) | 설정된 규칙에 따라 방화벽 감시를 시작함 | 보안 시스템 가동 |
| **`--force`** | 강제로 (Force) | 실행 중 묻는 질문(Y/N)을 생략하고 즉시 실행 | 즉각적인 명령 수행 |
| **`verbose`** | 상세히 (Verbose) | 기본 정보보다 더 자세한 설정 내역을 보고 싶을 때 사용 | 상세 로그 출력 |

---

### 💡 요약
이 과정은 **"우리 집 출입 통제소(UFW)를 설치하고, 우리가 새로 만든 비밀문(20022)과 서비스문(15034)만 통과할 수 있도록 명부에 등록한 뒤, 경비원에게 정식 근무를 시작(enable)하라고 명령하는 것"**과 같습니다.  

<img width="478" height="307" alt="스크린샷 2026-05-14 오후 10 28 35" src="https://github.com/user-attachments/assets/90e781b6-f1d6-4782-a8b6-27ede1256deb" />
  
방화벽 규칙을 적용하고, 시스템 부팅 시에도 자동으로 방화벽이 작동하도록 설정한 최종 확인 데이터입니다.

#### 1. 결과값 한 줄 해석

| 결과 데이터 (출력값) | 한 줄 해석 |
| :--- | :--- |
| **`Rules updated`** | 새로운 방화벽 규칙(포트 허용)이 시스템에 성공적으로 기록되었습니다. |
| **`Firewall is active...`** | 방화벽이 즉시 가동되었으며, 이제부터 **컴퓨터를 껐다 켜도 자동으로 실행**됩니다. |
| **`Status: active`** | 현재 방화벽 경비원이 입구에서 철저히 감시를 수행하고 있다는 뜻입니다. |
| **`20022/tcp ALLOW`** | SSH 전용 비밀문(20022)을 통해 들어오는 접속을 **허용(ALLOW)**합니다. |
| **`15034/tcp ALLOW`** | 특정 서비스용 문(15034)을 통해 들어오는 접속을 **허용(ALLOW)**합니다. |
| **`(v6)`** | 최신 주소 체계인 IPv6 환경에서도 동일한 규칙이 적용되고 있음을 뜻합니다. |
| **`Anywhere`** | 접속자의 위치(IP)에 상관없이, 올바른 문 번호로 오면 들여보내겠다는 의미입니다. |

---

#### 🔍 항목별 상세 의미 (구조적 분석)

* **`To` (대상)**: 외부에서 우리 서버의 **어떤 문(Port)**으로 들어오려고 하는지를 나타냅니다.
* **`Action` (동작)**: 해당 요청을 어떻게 처리할지 결정합니다. `ALLOW`는 통과, 기록에 없는 나머지는 모두 자동 차단됩니다.
* **`From` (출발지)**: 접속을 시도하는 사람의 위치입니다. `Anywhere`는 전 세계 어디서든 접속 가능하다는 뜻입니다.

---

#### 💡 최종 결론
이 데이터는 **"서버의 모든 구멍을 막아버린 상태에서, 우리가 약속한 20022번과 15034번 문만 정식으로 개방했음"**을 증명합니다. 이제 허가받지 않은 해커가 다른 문으로 들어오려고 하면 방화벽이 입구에서 즉시 차단하게 됩니다.

##

### ⁂계정/그룹 생성 확인   
`id agent-admin`, `id agent-dev`  
```bash
# 1. 그룹 생성
sudo groupadd agent-common
sudo groupadd agent-core

# 2. 사용자 생성 (홈 디렉토리 생성 및 보조 그룹 할당)
sudo useradd -m -G agent-core,agent-common agent-admin
sudo useradd -m -G agent-core,agent-common agent-dev
sudo useradd -m -G agent-common agent-test

# 3. 생성 확인 (정상이라면 각 계정 정보가 한 줄씩 출력됨)
id agent-admin && id agent-dev && id agent-test

```
## 👥 사용자 및 그룹 관리 (Identity & Access Management)

서버라는 요새에 누가 들어올 수 있는지 정하고, 각자 어떤 방(폴더)에 접근할 수 있는지 권한을 부여하는 보안의 기초 작업입니다.

---

### 🧐 계정/그룹 생성을 확인해야 하는 이유 (개념 설명)

1. **권한 분리의 원칙**: 모든 사람이 관리자 권한을 가질 필요는 없습니다. 개발자, 테스터, 관리자에게 각기 다른 그룹을 할당하여 서로의 영역을 침범하지 못하게 합니다.
2. **사고 예방**: 잘못된 그룹에 할당되면 중요한 시스템 파일을 삭제하거나 설정을 망가뜨릴 수 있습니다. 생성 직후 확인하는 과정은 '열쇠가 제대로 배정되었는지' 검사하는 절차입니다.
3. **추적성(Auditing)**: 서버에 문제가 생겼을 때 어떤 계정이 수행한 작업인지 명확히 알기 위해 개별 계정 생성은 필수입니다.

---

### 📝 명령어 한 줄 해석 (언제, 어디서, 어떻게, 왜)

1. `sudo groupadd agent-common`
   * **사용 구조**: `groupadd [그룹명]`
   * **언제/어디서**: 공통적인 권한이 필요한 사용자들을 묶어주기 위해 터미널에서 실행합니다.
   * **어떻게/왜**: `agent-common`이라는 그룹을 생성하여, 여러 사용자가 공통 자원에 접근할 수 있는 '울타리'를 만듭니다.

2. `sudo groupadd agent-core`
   * **사용 구조**: `groupadd [그룹명]`
   * **언제/어디서**: 핵심 시스템 접근 권한이 필요한 핵심 인력을 위해 실행합니다.
   * **어떻게/왜**: `agent-core` 그룹을 만들어 중요 데이터에 접근할 수 있는 차별화된 권한 그룹을 준비합니다.

3. `sudo useradd -m -G agent-core,agent-common agent-admin`
   * **사용 구조**: `useradd [옵션] [계정명]`
   * **언제/어디서**: 새로운 관리자(admin)를 시스템에 등록할 때 사용합니다.
   * **어떻게/왜**: 홈 디렉토리를 생성(`-m`)하고, 핵심(`core`)과 공통(`common`) 그룹에 동시에 소속시켜 모든 관리 권한을 부여합니다.

4. `sudo useradd -m -G agent-core,agent-common agent-dev`
   * **사용 구조**: `useradd [옵션] [계정명]`
   * **언제/어디서**: 개발 업무를 담당할 신규 사용자를 등록할 때 사용합니다.
   * **어떻게/왜**: 관리자와 동일한 그룹 권한을 주어 개발 및 시스템 핵심 제어가 가능하도록 계정을 생성합니다.

5. `sudo useradd -m -G agent-common agent-test`
   * **사용 구조**: `useradd [옵션] [계정명]`
   * **언제/어디서**: 테스트만 담당할 제한적 권한의 사용자를 등록할 때 사용합니다.
   * **어떻게/왜**: 핵심 그룹(`core`)은 제외하고 공통 그룹(`common`)에만 포함시켜 접근 범위를 제한합니다.

6. `id agent-admin && id agent-dev && id agent-test`
   * **사용 구조**: `id [계정명] && [다음명령어]`
   * **언제/어디서**: 계정 생성이 모두 끝난 직후, 설정이 의도대로 되었는지 확인할 때 사용합니다.
   * **어떻게/왜**: 각 계정의 UID, GID, 소속 그룹 정보를 한눈에 출력하여 설정 오류가 없는지 최종 검증합니다.

---

### 🔍 명령어 상세 분석 (약자 및 의미)

| 명령어/옵션 | 약자 및 뜻 | 사용법 및 왜 사용하는가? | 결과 |
| :--- | :--- | :--- | :--- |
| **`sudo`** | **S**uper**U**ser **DO** | 최고 관리자 권한으로 명령을 실행할 때 사용 | 관리자 실행 권한 획득 |
| **`groupadd`** | **Group Add** | 시스템에 새로운 그룹을 추가할 때 사용 | 신규 그룹 생성 |
| **`useradd`** | **User Add** | 시스템에 새로운 사용자 계정을 추가할 때 사용 | 신규 사용자 생성 |
| **`-m`** | **m**ake home | 사용자의 전용 공간인 홈 디렉토리(`/home/계정명`)를 자동 생성 | 개인 폴더 생성 |
| **`-G`** | **G**roups (supplementary) | 기본 그룹 외에 추가로 소속될 보조 그룹들을 지정 | 다중 그룹 권한 부여 |
| **`id`** | **id**entification | 특정 사용자의 식별 정보(UID, GID, Groups)를 확인 | 계정 정보 출력 |
| **`&&`** | **And** (논리 연산자) | 앞의 명령이 성공했을 때만 다음 명령을 이어서 실행 | 연속 명령 실행 |

---

### 🧩 구조적 분석 (구조도)

명령어가 작동하는 논리적 구조입니다.

* **그룹 생성**: `groupadd` + `[생성할 그룹 이름]`
* **사용자 생성**: `useradd` + `[홈폴더 생성 옵션]` + `[보조그룹 목록 옵션]` + `[새 사용자 이름]`
* **논리 연결**: `[명령어 A]` + `&&` + `[명령어 B]` (A가 성공하면 B 실행)

---

### 💡 최종 요약
이 과정은 **"먼저 '관리팀'과 '개발팀'이라는 부서를 만들고(groupadd), 각 직원의 개인 책상을 마련해주면서 해당 부서에 배치한 뒤(useradd), 마지막으로 사원증이 제대로 발급되었는지 확인(id)하는 과정"**과 같습니다.

<img width="642" height="254" alt="스크린샷 2026-05-14 오후 10 32 20" src="https://github.com/user-attachments/assets/0b053376-c2e3-44d4-b3bd-5bab5d074bf4" />

#### 1. 결과값 한 줄 해석 (시스템 메시지 및 출력값)

| 결과 데이터 (출력값) | 한 줄 해석 |
| :--- | :--- |
| **`already exists`** | 해당 그룹이나 사용자가 이미 시스템에 생성되어 있어 중복 생성을 방지했다는 뜻입니다. |
| **`uid=1000(agent-admin)`** | `agent-admin` 사용자의 고유 번호(주민번호와 유사)가 1000번으로 지정되었습니다. |
| **`gid=1000(agent-common)`** | 이 사용자의 기본 소속 부서(그룹)가 `agent-common`임을 나타냅니다. |
| **`groups=...`** | 사용자가 현재 가지고 있는 모든 출입 권한(그룹) 목록을 보여줍니다. |
| **`1001(agent-core)`** | 보조 그룹 옵션(`-G`)을 통해 핵심 권한 그룹에 성공적으로 포함되었음을 증명합니다. |

---

#### 🔍 `id` 명령어 결과 상세 분석 (구조적 분석)

가장 하단의 출력값 중 `agent-admin`의 예시를 통해 기호와 숫자의 의미를 뜯어보겠습니다.

* **`uid (User ID)`**: 시스템이 사용자를 식별하기 위한 고유 숫자입니다. (이름은 사람이 보기 편하게 붙인 별칭일 뿐입니다.)
* **`gid (Group ID)`**: 사용자가 생성될 때 할당된 기본 그룹의 식별 번호입니다.
* **`groups (Groups)`**: 현재 이 사용자가 발급받은 **모든 사원증(권한)** 목록입니다.
    * `agent-admin`: common(1000)과 core(1001) **둘 다** 가짐 (관리자)
    * `agent-dev`: common(1000)과 core(1001) **둘 다** 가짐 (개발자)
    * `agent-test`: common(1000) **하나만** 가짐 (테스터 - core 권한 없음)

---

#### 🧩 명령어 사용 구조 (Structure)

* **확인 구조**: `id` + `[확인할 사용자 이름]`
* **연속 실행**: `[명령어1]` + `&&` + `[명령어2]`
    * 의미: "1번 확인이 성공하면 2번을 확인해라!"라는 뜻으로, 여러 계정을 한 번에 검수할 때 효율적입니다.

---

#### 💡 최종 결론
이미지의 결과는 **"관리자(admin)와 개발자(dev)에게는 핵심(core) 권한까지 부여되었고, 테스터(test)에게는 공통(common) 권한만 부여되어 기획된 보안 정책이 완벽하게 적용되었음"**을 보여주는 최종 합격 통지서입니다.

##

### ⁂디렉토리 구조 및 ACL 권한  
`ls -ld`, `getfacl [디렉토리명]`  
```bash
# 1. 디렉토리 생성
sudo mkdir -p /home/agent-admin/agent-app/upload_files
sudo mkdir -p /home/agent-admin/agent-app/api_keys
sudo mkdir -p /var/log/agent-app

# 2. 소유권 설정 (admin 소유, common 그룹 관리)
sudo chown -R agent-admin:agent-common /home/agent-admin/agent-app

# 3. 상세 권한 설정 (핵심!)
# api_keys와 log는 core 그룹(admin, dev)만 볼 수 있게 설정
sudo chown :agent-core /home/agent-admin/agent-app/api_keys /var/log/agent-app
sudo chmod 770 /home/agent-admin/agent-app/upload_files
sudo chmod 770 /home/agent-admin/agent-app/api_keys
sudo chmod 770 /var/log/agent-app

# 4. 검증
sudo ls -ld /home/agent-admin/agent-app/upload_files
sudo ls -ld /home/agent-admin/agent-app/api_keys
sudo ls -ld /var/log/agent-app
```
## 📂 디렉토리 구조 및 권한 관리 (Directory & Permission)

시스템 내에서 파일이 저장될 물리적 위치를 정의하고, 각 사용자의 역할에 따라 접근 권한을 세밀하게 통제하는 작업입니다.

---

### 🧐 개념 설명

#### 1. 디렉토리 및 구조 (Directory Structure)
* **개념**: 파일을 용도별로 분류하여 저장하는 '폴더'의 집합입니다. 리눅스는 모든 것이 파일로 관리되므로, 체계적인 디렉토리 구조는 관리 효율성을 결정짓습니다.
* **현재 구조 분석**:
    * `/home/agent-admin/agent-app/`: 애플리케이션의 메인 저장소입니다.
    * `.../upload_files`: 사용자가 업로드한 파일이 담기는 공용 공간입니다.
    * `.../api_keys`: 보안 키가 보관되는 **민감한 공간**입니다.
    * `/var/log/agent-app`: 앱 실행 기록(Log)을 저장하는 표준 위치입니다.

#### 2. ACL (Access Control List) 권한
* **개념**: 기본 권한(소유자/그룹/기타)만으로는 부족할 때 사용하는 **'정밀 접근 제어 목록'**입니다.
* **필요성**: 특정 사용자나 그룹에게만 예외적으로 권한을 부여하고 싶을 때(예: 특정 팀원 한 명에게만 읽기 허용) 사용합니다.
* **기능**: 기본 그룹 정책을 깨지 않고도 개별적인 '특별 출입증'을 발급하는 역할을 합니다. `ls -l` 시 권한 끝에 **`+` 기호**가 붙으면 ACL이 적용된 상태입니다.

---

### 📝 명령어 한 줄 해석 (언제, 어디서, 어떻게, 왜)

1. `sudo mkdir -p /home/agent-admin/agent-app/upload_files`
   * **구조**: `mkdir [옵션] [경로]`
   * **해석**: 부모 디렉토리가 없어도 한 번에 생성(`-p`)하여 데이터 저장 공간을 확보합니다.

2. `sudo chown -R agent-admin:agent-common /home/agent-admin/agent-app`
   * **구조**: `chown [옵션] [소유자]:[그룹] [경로]`
   * **해석**: 하위 폴더까지 싹 다(`-R`) 관리자 소유로 바꿔서 관리 권한을 명확히 합니다.

3. `sudo chown :agent-core /home/agent-admin/agent-app/api_keys /var/log/agent-app`
   * **구조**: `chown :[그룹] [경로]`
   * **해석**: 민감한 폴더의 관리 부서를 핵심 인력(`agent-core`)으로 한정합니다.

4. `sudo chmod 770 /home/agent-admin/agent-app/upload_files`
   * **구조**: `chmod [권한숫자] [경로]`
   * **해석**: 주인과 그룹은 모든 권한(`7`)을 갖지만, 외부인(Others)은 아예 접근 못 하게(`0`) 차단합니다.

5. `sudo ls -ld /home/agent-admin/agent-app/upload_files`
   * **구조**: `ls -ld [경로]`
   * **해석**: 디렉토리 자체의 상세 정보(`-d`, `-l`)를 출력하여 권한 설정 결과를 검증합니다.

---

### 🔍 명령어 상세 분석 (약자 및 의미)

| 명령어/옵션 | 뜻 (Full Name) | 용도 | 결과 |
| :--- | :--- | :--- | :--- |
| **`mkdir`** | **M**ake **Dir**ectory | 폴더 생성 | 디렉토리 생성 |
| **`chown`** | **CH**ange **OWN**er | 소유자/그룹 변경 | 주인 및 관리그룹 변경 |
| **`chmod`** | **CH**ange **MOD**e | 읽기/쓰기/실행 권한 설정 | 보안 출입 통제 |
| **`setfacl`** | **set** **f**ile **ACL** | 특정 인원에게 예외 권한 부여 | 정밀 권한 설정 |
| **`getfacl`** | **get** **f**ile **ACL** | 설정된 상세 ACL 명단 확인 | 상세 권한 리스트 확인 |

---

### 💡 최종 요약
이 과정은 **"필요한 비밀 방을 만들고(mkdir), 주인과 부서를 정한 뒤(chown), 770 도어락을 달아(chmod) 허락된 멤버만 들어오게 설정하고, 필요한 경우 특정인에게 별도 열쇠를 주는(ACL) 보안 완결 과정"**입니다.
<img width="629" height="158" alt="스크린샷 2026-05-14 오후 10 39 08" src="https://github.com/user-attachments/assets/96d15249-dcad-4ee3-a407-0314df4437fc" />

<img width="665" height="97" alt="스크린샷 2026-05-14 오후 10 39 21" src="https://github.com/user-attachments/assets/edf7f5e3-1770-4787-92b5-8928a13e1cc9" />

서버 인프라 구축의 핵심 3단계(SSH, 방화벽, 디렉토리 권한)가 설계대로 완벽하게 적용되었음을 확인하였습니다.

---

### 1. SSH 및 네트워크 상태 분석 (ss -tuln)
**목적**: 변경된 비밀 포트(20022)가 외부 접속을 받을 준비가 되었는지 확인

| 항목 | 출력값 | 의미 | 상태 |
| :--- | :--- | :--- | :---: |
| **Protocol** | `tcp` | 신뢰성 기반의 연결 지향형 통신 사용 | 정상 |
| **State** | `LISTEN` | 서비스가 활성화되어 접속 대기 중 | **Active** |
| **Local Port** | `:20022` | 기본 22번 포트 차단 및 **비밀 포트 전환 완료** | **성공** |
| **Address** | `0.0.0.0` / `[::]` | IPv4 및 IPv6 모든 주소체계 접속 허용 | 정상 |

---

### 2. 방화벽 운용 상태 분석 (ufw status)
**목적**: 허용된 포트 외의 모든 무차별 대입 공격 차단 확인

| 대상 포트 (To) | 동작 (Action) | 출발지 (From) | 비고 |
| :--- | :--- | :--- | :--- |
| **20022/tcp** | **ALLOW** | Anywhere | SSH 관리 전용 통로 개방 |
| **15034/tcp** | **ALLOW** | Anywhere | 어플리케이션 서비스 포트 개방 |
| **Status: active** | - | - | **방화벽 시스템 정상 가동 중** |

---

### 3. 디렉토리 보안 및 소유권 분석 (ls -ld)
**목적**: 사용자 그룹(Core, Common)별 접근 권한이 물리적으로 격리되었는지 확인

| 디렉토리 경로 | 권한(Permission) | 소유자:그룹 | 보안 수준 및 의미 |
| :--- | :--- | :--- | :--- |
| `/.../upload_files` | `drwxrwx---` (770) | `admin:common` | 관리자 및 공통그룹(Dev, Test) 협업 공간 |
| `/.../api_keys` | `drwxrwx---` (770) | `admin:core` | **최상위 보안**: 핵심 인력(Admin, Dev) 전용 |
| `/var/log/agent-app` | `drwxrwx---` (770) | `admin:core` | 시스템 로그 유출 방지 및 핵심 인력 관리 |

#### 🔍 권한 기호 상세 해석 (`drwxrwx---`)
* **`d`**: 디렉토리(폴더)임을 의미합니다.
* **`rwx` (첫 번째)**: 소유자(`agent-admin`)에게 모든 권한 부여.
* **`rwx` (두 번째)**: 할당된 그룹(`common` 또는 `core`)에게 모든 권한 부여.
* **`---` (세 번째)**: **외부인(Others)은 절대 접근 불가.**

---

### 💡 최종 검증 결론
모든 검증 지표가 **Green(정상)** 상태입니다. 
- **외부 보안**: SSH 포트 은닉 및 방화벽 필터링을 통해 1차 방어선 구축 완료.
- **내부 보안**: 그룹별 디렉토리 권한 할당을 통해 2차 내부 통제 시스템 구축 완료.
##

### ⁂앱 Boot Sequence 5단계 [OK]  
`Agent READY` 출력 확인  

```bash
# 1단계
# 소유권 설정
sudo chown agent-admin:agent-common /home/agent-admin/agent-app/agent-app
  
# 실행 권한 부여 (확장자가 없으므로 실행 가능하게 만들어야 함)
sudo chmod +x /home/agent-admin/agent-app/agent-app

#검증
# 1 단계 -u 옵션을 사용하여 agent-admin 계정으로 앱을 실행합니다.  
sudo -u agent-admin /home/agent-admin/agent-app/agent-app | sudo tee /var/log/agent-app/boot.log
```
## 🚀 앱 Boot Sequence (부팅 시퀀스) 상세 분석

애플리케이션이 서버 자원을 사용하기 전에 권한을 점검하고, 로그를 생성하며, 안정적으로 시스템에 안착하는 일련의 과정입니다.

---

### 🧐 개념 설명

#### 1. Boot Sequence 5단계란? (전체 흐름)
단순히 앱을 '더블클릭'해서 실행하는 것이 아니라, 서버 환경에 맞게 다음 단계를 거칩니다.
1. **권한 정립(Privilege Setup)**: 앱 파일의 주인과 실행 권한을 설정합니다. (현재 단계)
2. **환경 변수 로드(Env Loading)**: API 키나 DB 주소 등 설정값을 불러옵니다.
3. **리소스 점검(Dependency Check)**: 필요한 라이브러리나 네트워크 포트가 준비되었는지 확인합니다.
4. **프로세스 실행(Execution)**: 지정된 사용자 계정으로 앱을 백그라운드에서 띄웁니다.
5. **헬스 체크(Health Check)**: 앱이 정상적으로 응답하는지 확인하고 로그를 남깁니다.

#### 2. 왜 이 과정이 필요한가요?
* **보안 강화**: 관리자(`root`) 권한으로 앱을 돌리면 해킹 시 서버 전체가 위험해집니다. 따라서 낮은 권한의 전용 계정(`agent-admin`)으로 실행해야 합니다.
* **추적 가능성**: 실행 과정을 로그(`boot.log`)로 남겨야 나중에 앱이 죽었을 때 왜 죽었는지 원인을 찾을 수 있습니다.

---

### 📝 명령어 한 줄 해석 (언제, 어디서, 어떻게, 왜)

1. `sudo chown agent-admin:agent-common /home/agent-admin/agent-app/agent-app`
   * **사용 구조**: `chown [소유자]:[그룹] [파일경로]`
   * **언제/어디서**: 앱 파일을 서버에 업로드한 직후, 터미널에서 실행합니다.
   * **어떻게/왜**: 앱 실행 파일의 주인을 `agent-admin`으로 지정하여, 이 계정만이 앱을 제어할 수 있게 물리적 소유권을 확정합니다.

2. `sudo chmod +x /home/agent-admin/agent-app/agent-app`
   * **사용 구조**: `chmod +x [파일경로]`
   * **언제/어디서**: 소유권 설정 후, 해당 파일을 '단순 문서'가 아닌 '실행 프로그램'으로 인식시켜야 할 때 실행합니다.
   * **어떻게/왜**: 리눅스는 확장자(`.exe` 등)가 없어도 실행 권한(`x`)이 있어야만 프로그램이 돌아갑니다. 이 명령으로 앱에 '생명력'을 불어넣습니다.

3. `sudo -u agent-admin /home/agent-admin/agent-app/agent-app | sudo tee /var/log/agent-app/boot.log`
   * **사용 구조**: `sudo -u [사용자] [명령어] | sudo tee [로그경로]`
   * **언제/어디서**: 모든 준비를 마치고 실제 앱을 가동하여 검증할 때 실행합니다.
   * **어떻게/왜**: `-u` 옵션으로 관리자가 아닌 일반 계정인 척 앱을 실행하고, 화면에 나오는 출력 내용을 가로채서 로그 파일(`boot.log`)에 실시간으로 기록합니다.

---

### 🔍 명령어 상세 분석 (약자 및 의미)

| 명령어/옵션 | 약자 및 뜻 | 사용법 및 왜 사용하는가? | 결과 |
| :--- | :--- | :--- | :--- |
| **`chown`** | **CH**ange **OWN**er | 파일의 주인과 그룹을 바꿀 때 사용 | 파일 소유권 이전 |
| **`chmod`** | **CH**ange **MOD**e | 파일의 읽기/쓰기/실행 모드를 변경 | 실행 가능 상태로 변경 |
| **`+x`** | plus e**X**ecute | 파일에 실행(Execute) 권한을 부여 | 프로그램 가동 준비 완료 |
| **`sudo -u`** | **S**uper**U**ser **DO** **U**ser | 특정 사용자(User)의 신분으로 명령을 실행 | 하위 권한 실행(보안) |
| **`|`** (파이프) | Pipe | 앞 명령의 결과를 뒷 명령의 입력으로 전달 | 데이터 연결 |
| **`tee`** | T (알파벳 T 모양) | 출력 내용을 화면에도 보여주고 파일에도 저장 | 실시간 로그 기록 |

---

### 🧩 구조적 분석 (Structure)

* **소유권 구조**: `chown` + `[사용자]:[그룹]` + `[대상]`
* **권한 구조**: `chmod` + `[연산자(+)][권한(x)]` + `[대상]`
* **실행 및 기록 구조**: `[실행명령]` + `|` + `tee` + `[저장경로]`

---

### 💡 최종 요약
이 과정은 **"프로그램이라는 기계의 주인을 정하고(chown), 전원을 켤 수 있게 버튼을 활성화한 뒤(chmod +x), 관리자가 아닌 담당 직원의 이름으로 기계를 가동하면서 그 과정을 블랙박스에 기록(tee)하는 절차"**입니다.

<img width="772" height="29" alt="스크린샷 2026-05-14 오후 11 21 01" src="https://github.com/user-attachments/assets/176efd82-9d12-4ccb-a6b0-7455053c8e54" />
<img width="811" height="217" alt="스크린샷 2026-05-14 오후 11 21 54" src="https://github.com/user-attachments/assets/ec873522-06ec-4071-9d89-9316ceeca602" />
  
#### 1. [OK] 단계: 사용자 계정 검증 (Checking User Account)
* **내용**: `Running as service user 'agent-admin' (uid=1000)`
* **해석**: 앱이 관리자(root)가 아닌, 우리가 미리 생성해둔 **`agent-admin` 계정의 신분으로 실행되는 데 성공**했습니다.
* **의미**: 보안 가이드라인에 따라 적절한 신분증(UID 1000)을 지참하고 성안에 들어온 상태입니다.

---

#### 2. [FAIL] 발생 원인: 환경 변수 누락 (Critical Env 'AGENT_HOME' is missing)
* **원인**: 앱이 자신의 고향(홈 디렉토리)이 어디인지 기록된 **`AGENT_HOME`이라는 환경 변수**를 찾지 못했습니다.
* **결과**: 이 정보는 앱 구동에 있어 '필수 정보'이기 때문에 여기서 첫 번째 실패가 발생했습니다.

---

#### 3. 나머지 [FAIL]들의 원인 (Skipped due to previous critical failure)
* **대상**: 3단계(파일 체크), 4단계(포트 확인), 5단계(로그 권한)
* **해석**: 2단계에서 **치명적인 오류(Critical Failure)**가 발생했기 때문에, 시스템이 더 이상 검사를 진행하는 것이 무의미하다고 판단하여 이후 단계를 모두 **건너뛰기(Skip)** 한 것입니다.
* **비유**: 자동차 시동을 걸었는데 엔진(환경 변수)이 없어서, 타이어가 있는지나 라이트가 켜지는지를 확인하지 않고 바로 멈춰버린 상황과 같습니다.

---

다음 단계로 넘어가려면 앱이 참조할 수 있도록 **환경 변수(`AGENT_HOME`)를 설정**해주는 작업이 필요합니다. 2단계를 고치면 도미노처럼 나머지 3, 4, 5단계 검사도 차례대로 진행될 것입니다.

```bash
# 2 단계 / 3단계
#앱이 원하는 업로드 폴더 생성
sudo mkdir -p /home/agent-admin/agent-app/upload_files

#앱이 원하는 키 폴더 및 파일 생성
sudo mkdir -p /home/agent-admin/agent-app/api_keys
sudo touch /home/agent-admin/agent-app/api_keys/t_secret.key

#소유권 전체 재설정
sudo chown -R agent-admin:agent-common /home/agent-admin/agent-app/

#검증
sudo -u agent-admin \
AGENT_HOME=/home/agent-admin/agent-app \
AGENT_PORT=15034 \
AGENT_UPLOAD_DIR=/home/agent-admin/agent-app/upload_files \
AGENT_KEY_PATH=/home/agent-admin/agent-app/api_keys/t_secret.key \
/home/agent-admin/agent-app/agent-app | sudo tee /var/log/agent-app/boot.log
```
## 🚀 앱 Boot Sequence 2~3단계: 환경 구성 및 리소스 준비

앱이 실행되는 데 필요한 경로와 파일을 생성하고, 이를 앱이 인식할 수 있도록 환경 변수를 주입하는 과정입니다.

---

### 📝 명령어 한 줄 해석 (언제, 어디서, 어떻게, 왜)

1. `sudo mkdir -p /home/agent-admin/agent-app/upload_files`
   * **사용 구조**: `mkdir [옵션] [경로]`
   * **언제/어디서**: 앱 구동 전, 데이터가 저장될 물리적 공간이 필요할 때 실행합니다.
   * **어떻게/왜**: `-p` 옵션으로 상위 디렉토리까지 한 번에 생성하여 앱이 업로드 파일을 저장할 장소를 마련합니다.

2. `sudo mkdir -p /home/agent-admin/agent-app/api_keys`
   * **사용 구조**: `mkdir [옵션] [경로]`
   * **언제/어디서**: 앱의 보안 인증에 필요한 키 뭉치를 보관할 방을 만들 때 실행합니다.
   * **어떻게/왜**: 민감한 정보인 API 키를 일반 파일과 분리하여 관리하기 위해 전용 폴더를 생성합니다.

3. `sudo touch /home/agent-admin/agent-app/api_keys/t_secret.key`
   * **사용 구조**: `touch [파일경로]`
   * **언제/어디서**: 앱이 부팅 시 체크하는 '필수 파일'이 실제로 존재해야 할 때 실행합니다.
   * **어떻게/왜**: 빈 파일을 생성하여 앱이 "어? 열쇠 파일이 없네?"라고 하며 부팅을 포기하지 않도록 만듭니다.

4. `sudo chown -R agent-admin:agent-common /home/agent-admin/agent-app/`
   * **사용 구조**: `chown [옵션] [소유자]:[그룹] [경로]`
   * **언제/어디서**: 새로운 파일과 폴더를 생성한 직후, 권한을 일치시키기 위해 실행합니다.
   * **어떻게/왜**: 새로 만든 `upload_files`와 `t_secret.key`의 주인을 앱 실행 계정인 `agent-admin`으로 일괄 변경합니다.

5. `sudo -u agent-admin ... /home/agent-admin/agent-app/agent-app | sudo tee ...`
   * **사용 구조**: `sudo -u [사용자] [환경변수명=값] [실행파일] | sudo tee [로그경로]`
   * **언제/어디서**: 모든 준비가 끝난 후, 앱에 필요한 정보를 주입하며 실제로 가동할 때 실행합니다.
   * **어떻게/왜**: `AGENT_HOME` 등의 환경 변수를 실시간으로 전달하여 앱이 앞서 만든 폴더와 파일들의 위치를 정확히 알게 합니다.

---

### 🔍 명령어 상세 분석 (약자 및 의미)

| 명령어/옵션 | 약자 및 뜻 | 사용법 및 왜 사용하는가? | 결과 |
| :--- | :--- | :--- | :--- |
| **`mkdir -p`** | **M**ake **Dir**ectory **P**arents | 하위 폴더를 포함해 한꺼번에 생성할 때 사용 | 경로 자동 생성 완료 |
| **`touch`** | **Touch** (만지다) | 내용이 없는 빈 파일을 생성하거나 수정 시간을 갱신 | 0바이트 파일 생성 |
| **`chown -R`** | **CH**ange **OWN**er **R**ecursive | 하위 모든 파일/폴더의 소유권을 한 번에 바꿀 때 사용 | 하위 권한 일괄 변경 |
| **`AGENT_HOME`** | **Environment Variable** | 앱에게 "너의 집(메인 경로)은 여기야"라고 알려주는 변수 | 앱이 경로 인식 성공 |
| **`\`** (역슬래시) | **Line Continuation** | 너무 긴 명령어를 가독성 좋게 여러 줄로 나누어 작성 | 한 줄 명령어로 인식 |
| **`tee`** | **T**-junction (T자 분기점) | 데이터 흐름을 나눠서 화면 출력과 파일 저장을 동시에 수행 | 실시간 확인 및 기록 |

---

### 🧩 구조적 분석 (Structure)

* **파일 생성 구조**: `touch` + `[생성할 파일 이름 및 경로]`
* **환경 변수 주입 구조**: `[변수명]=[값]` + `[변수명2]=[값2]` + `[실행파일]`
* **로그 분기 구조**: `[실행명령어]` + `|` + `sudo tee` + `[로그파일]`

---

### 💡 최종 요약
이 과정은 **"앱이 살 집(폴더)과 출입 카드(파일)를 새로 만들어주고(mkdir/touch), 모든 물건의 명의를 앱 주인으로 돌려놓은 뒤(chown -R), 실행 시 주소와 포트 번호를 적은 안내판(환경 변수)을 앱에게 쥐여주며 가동시키는 과정"**입니다.

<img width="745" height="383" alt="스크린샷 2026-05-14 오후 11 30 32" src="https://github.com/user-attachments/assets/28b2ff30-48ba-4aed-bc67-51b1f1f5f18b" />

#### 1. [OK] 성공 단계 (1~3단계)
* **[1/5] Checking User Account**: 앱 실행 주체가 `agent-admin`임을 다시 한번 확인했습니다.
* **[2/5] Verifying Environment Variables**: 
    * **해석**: `AGENT_HOME`, `AGENT_PORT` 등 우리가 명령줄에 길게 썼던 설정값들이 앱에 정확히 전달되었습니다.
    * **의미**: 앱이 이제 "내가 어디에 있고, 몇 번 포트를 써야 하는지" 완벽히 인지했습니다.
* **[3/5] Checking Required Files**:
    * **해석**: `touch`로 만들었던 `t_secret.key` 파일을 앱이 성공적으로 찾아내고 검증했습니다.
    * **의미**: 앱 구동에 필요한 '열쇠'가 물리적으로 존재함을 확인했습니다.

---

#### 2. [FAIL] 발생 원인: 포트 충돌 (Port 15034 is already in use)
* **원인**: 앱이 사용하려는 **15034번 포트**를 이미 다른 프로세스(또는 이전에 실행했던 앱의 잔재)가 점유하고 있습니다.
* **비유**: 손님이 15034번 방(Port)에 들어가려고 하는데, 이미 다른 사람이 방을 쓰고 있어서 들어가지 못하는 상황입니다.
* **결과**: 앱이 네트워크 통신을 시작할 수 없으므로 4단계에서 실패하게 됩니다.

---

#### 3. 5단계 [FAIL]의 원인 (Skipped due to previous critical failure)
* **해석**: 4단계(포트 확인)에서 치명적인 네트워크 오류가 발생했기 때문에, 마지막 단계인 로그 권한 확인은 시도하지 않고 **건너뛰기(Skip)** 되었습니다.

---

### 💡 해결을 위한 팁 (포트 충돌 해결법)
현재 앱이 3단계까지 성공했다는 것은 **파일과 환경 설정은 완벽하다**는 뜻입니다! 4단계 실패를 해결하려면 다음 명령어로 15034번 포트를 누가 쓰고 있는지 확인하고 종료해야 합니다.

---

### 🏆 전체 요약
계정 생성(1단계) → 환경 변수(2단계) → 필수 파일(3단계)

```bash
# 4단계 / 5단계
1. 15034 포트를 쓰고 있는 범인 찾기
sudo ss -tulpn | grep 15034

1. 범인 소탕 (프로세스 종료)
sudo kill -9 2396

#검증
sudo -u agent-admin \
AGENT_HOME=/home/agent-admin/agent-app \
AGENT_PORT=15034 \
AGENT_UPLOAD_DIR=/home/agent-admin/agent-app/upload_files \
AGENT_KEY_PATH=/home/agent-admin/agent-app/api_keys/t_secret.key \
/home/agent-admin/agent-app/agent-app | sudo tee /var/log/agent-app/boot.log

```

## 🚀 앱 Boot Sequence 4~5단계: 포트 확보 및 최종 가동

이미 점유된 포트를 찾아 프로세스를 강제 종료하고, 앱이 네트워크 환경에 정상적으로 안착하도록 만드는 과정입니다.

---

### 📝 명령어 한 줄 해석 (언제, 어디서, 어떻게, 왜)

1. `sudo ss -tulpn | grep 15034`
   * **사용 구조**: `ss [옵션] | grep [찾을문자열]`
   * **언제/어디서**: 포트 충돌(`Already in use`) 오류가 발생했을 때, 어떤 프로그램이 범인인지 찾기 위해 실행합니다.
   * **어떻게/왜**: 네트워크 상태(`ss`)를 상세히 조회하여 `15034`번 포트를 붙잡고 있는 프로세스 번호(PID)를 알아냅니다.

2. `sudo kill -9 2396`
   * **사용 구조**: `kill -[신호번호] [PID]`
   * **언제/어디서**: 포트를 점유 중인 범인(PID 2396)을 찾아낸 직후 실행합니다.
   * **어떻게/왜**: `-9`(강제 종료) 신호를 보내 해당 프로세스를 즉시 소탕하고, 앱이 사용할 15034번 포트를 빈 상태로 만듭니다.

3. `sudo -u agent-admin ... /home/agent-admin/agent-app/agent-app | sudo tee ...`
   * **사용 구조**: `sudo -u [사용자] [환경변수] [실행파일] | sudo tee [로그경로]`
   * **언제/어디서**: 포트가 비워진 것을 확인한 후, 최종적으로 앱을 구동할 때 실행합니다.
   * **어떻게/왜**: 앱에 필요한 모든 정보(환경변수)를 주입하며 가동하고, 그 결과가 성공(OK)인지 실시간으로 확인하며 로그에 박제합니다.

---

### 🔍 명령어 상세 분석 (약자 및 의미)

| 명령어/옵션 | 약자 및 뜻 | 사용법 및 왜 사용하는가? | 결과 |
| :--- | :--- | :--- | :--- |
| **`ss`** | **S**ocket **S**tatistics | 네트워크 소켓 상태를 확인하고 싶을 때 사용 | 연결 정보 리스트 출력 |
| **`-tulpn`** | **t**cp, **u**dp, **l**isten, **p**rocess, **n**umeric | 포트를 사용하는 프로그램 이름과 번호를 숫자로 표시 | 상세 포트 점유 현황 |
| **`grep`** | **G**lobally search a **R**egular **E**xpression and **P**rint | 방대한 출력 내용 중 특정 단어만 필터링할 때 사용 | 검색어 포함 줄만 출력 |
| **`kill`** | **Kill** (죽이다) | 실행 중인 특정 프로세스에 신호를 보내 종료시킴 | 프로세스 종료 |
| **`-9`** | **SIGKILL** (강제 종료 신호) | 프로그램이 스스로 종료되지 않을 때 시스템이 강제로 끔 | 즉각적인 프로세스 소멸 |
| **`2396`** | **P**rocess **ID** (PID) | 시스템이 각 프로그램에 부여한 고유 번호 (이름표) | 특정 대상 타겟팅 |

---

### 🧩 구조적 분석 (Structure)

* **점유 확인 구조**: `ss` + `[조회 옵션]` + `|` + `grep` + `[포트번호]`
* **프로세스 사살 구조**: `kill` + `[강제종정 신호(-9)]` + `[대상 PID]`
* **최종 앱 실행 구조**: `sudo -u [계정]` + `[환경변수들]` + `[앱경로]` + `|` + `tee` + `[로그]`

---

### 💡 최종 요약
이 과정은 **"15034번 방(포트)을 무단 점거하고 있는 범인을 네트워크 장부(ss)에서 찾아내고, 강제 퇴거 명령(kill -9)을 내려 방을 비운 뒤, 드디어 우리 앱이 모든 준비물(환경변수)을 챙겨서 방에 들어가 안정적으로 업무를 시작(tee)하게 만드는 마지막 보안 공정"**입니다.

<img width="754" height="453" alt="스크린샷 2026-05-14 오후 11 35 24" src="https://github.com/user-attachments/assets/4fb5dbe1-3d17-4d70-b438-7ea74b47a35e" />

#### 1. [OK] 1단계: 사용자 계정 검증 (User Account)
* **해석**: 앱이 보안 규정에 따라 `agent-admin` 계정(UID 1000)으로 정확히 실행되었습니다.
* **의미**: 관리자 권한 남용 없이, 지정된 담당자 신분으로 안전하게 활동을 시작했습니다.

#### 2. [OK] 2단계: 환경 변수 검증 (Environment Variables)
* **해석**: `AGENT_HOME`, `AGENT_PORT` 등 앱 운영에 필요한 모든 설정값이 오차 없이 주입되었습니다.
* **의미**: 앱이 본인의 위치와 통신 통로를 완벽하게 인지하고 동작할 준비를 마쳤습니다.

#### 3. [OK] 3단계: 필수 파일 검증 (Required Files)
* **해석**: `secret.key` 파일을 성공적으로 찾았고, 그 안의 키 내용까지 올바른 것을 확인했습니다.
* **의미**: 앱이 정상적으로 작동하기 위한 '보안 열쇠'가 유효함을 입증했습니다.

#### 4. [OK] 4단계: 포트 가용성 검증 (Port Availability)
* **해석**: `Port 15034 is available.`
* **의미**: 이전 단계에서 우리가 범인(PID 2396)을 소탕한 덕분에, 앱이 사용하려던 **15034번 통로가 비어있음을 확인**하고 점유에 성공했습니다.

#### 5. [OK] 5단계: 로그 권한 검증 (Log Permission)
* **해석**: 로그 저장소인 `/var/log/agent-app`에 글을 쓸 수 있는 권한이 있음을 확인했습니다.
* **의미**: 이제부터 발생하는 모든 활동 내역을 기록(블랙박스)할 수 있는 상태가 되었습니다.

---

### 🚀 최종 상태: Agent READY
* **결과**: `All Boot Checks Passed! / Agent READY`
* **로그 분석**: 
    * `Agent listening at port 15034`: 이제 외부에서 15034번 포트로 접속하면 응답할 준비가 되었습니다.
    * `Memory Increasing... (25 MB)`: 앱이 실제 메모리를 사용하며 활발히 작동하기 시작했습니다.

```bash
마지막검증
sudo cat /var/log/agent-app/boot.log
```
<img width="623" height="439" alt="스크린샷 2026-05-14 오후 11 41 56" src="https://github.com/user-attachments/assets/ac286607-6cde-415e-bfe2-26272ce46a10" />

##

### ⁂`monitor.sh` 실행 결과 프로세스/리소스 정상 수집 확인   

```bash
# 실행 권한 부여
sudo chmod 755 /home/agent-admin/agent-app/monitor.sh

# 소유권 설정 (계정과 그룹 확인)
sudo chown agent-admin:agent-common /home/agent-admin/agent-app/monitor.sh

# 3단계: crontab으로 1분 주기 자동화 등록
sudo crontab -u agent-admin -e
- # agent-admin 계정의 예약 작업 편집
* * * * * /home/agent-admin/agent-app/monitor.sh

#터미널에서 (* * * * * /home/agent-admin/agent-app/monitor.sh) 작동검사
sudo crontab -u agent-admin -ㅣ
```

<img width="675" height="69" alt="스크린샷 2026-05-15 오전 12 29 37" src="https://github.com/user-attachments/assets/4e73f9b2-7143-47ff-a67a-b326905bee7d" />
<img width="1061" height="493" alt="스크린샷 2026-05-14 오후 11 58 01" src="https://github.com/user-attachments/assets/3fe0287f-0632-4609-98e1-c5161adee18b" />
<img width="614" height="396" alt="스크린샷 2026-05-15 오전 12 01 05" src="https://github.com/user-attachments/assets/bdaa6043-8ecb-4e4d-b32c-9f9450cdf7ce" />

##

### ⁂`monitor.log` 누적 기록 / crontab 자동 실행 확인  (1분 주기 로그 증가 확인)  
[오류버전 ]
```bash
스크립트 실행  
`sudo -u agent-admin /home/agent-admin/agent-app/monitor.sh`  
로그확인  
`sudo tail -f /var/log/agent-app/monitor.log`
```
<img width="837" height="195" alt="스크린샷 2026-05-15 오전 12 58 43" src="https://github.com/user-attachments/assets/5aa2b7cf-144d-4588-8cf2-a6a40f5a27cb" />

##

[노멀버전] 
```bash
1. 프로세스 완전 종료 (찌꺼기 제거)
sudo pkill -f agent-app

2. 앱 다시 실행 (환경 변수 포함)
sudo -u agent-admin \
AGENT_HOME=/home/agent-admin/agent-app \
AGENT_PORT=15034 \
AGENT_UPLOAD_DIR=/home/agent-admin/agent-app/upload_files \
AGENT_KEY_PATH=/home/agent-admin/agent-app/api_keys/t_secret.key \
nohup /home/agent-admin/agent-app/agent-app > /dev/null 2>&1 &

3. 로그 확인
sudo tail -f /var/log/agent-app/monitor.log
```  
<img width="724" height="316" alt="스크린샷 2026-05-15 오전 1 03 01" src="https://github.com/user-attachments/assets/1cd775c4-0b1d-48d4-84fe-a5dc27f2a17b" />


---

## 3. 자동화 스크립트 소스코드 (Source Code)

    * 사람 대신 밤새 서버를 지켜볼 자동 감시 로봇(스크립트)을 만드는 단계
    - 생존 확인: "앱은 살아있나?", "문은 잘 열려있나?"를 먼저 확인합니다.
    - 정보 수집: CPU(두뇌 사용량), 메모리(작업 공간), 디스크(창고 저장량)가 얼마나 찼는지 기록합니다.
    - 위험 알림: 창고가 꽉 차거나 컴퓨터가 너무 뜨거워지면 로그에 [WARNING]이라고 적어 주인에게 알립니다.
    
### 📄 monitor.sh
```bash

# 1. 파일 생성 및 편집기 열기
sudo nano /home/agent-admin/agent-app/monitor.sh

#!/bin/bash

LOG_FILE="/var/log/agent-app/monitor.log"
APP_NAME="agent-app"
TARGET_PORT=15034
TIMESTAMP=$(date "+%Y-%m-%d %H:%M:%S")

# 1. 프로세스(PID) 및 포트 상태 확인
PID=$(pgrep -f "$APP_NAME" | head -n 1)
PORT_LISTEN=$(ss -tuln | grep -w "$TARGET_PORT")

# 2. 리소스 및 상태 판별
if [[ -n "$PID" ]]; then
    STATUS="UP"
    CPU=$(ps -p "$PID" -o %cpu --no-headers | xargs)
    MEM=$(ps -p "$PID" -o rss --no-headers | awk '{printf "%.2fMB", $1/1024}')
    
    # [포트 체크] 프로세스는 있는데 포트가 안 열려있으면 경고
    if [[ -z "$PORT_LISTEN" ]]; then
        PORT_STAT="OFFLINE"
        WARN="!! PORT_ERR !!"
    else
        PORT_STAT="ONLINE"
        WARN="NORMAL"
    fi
else
    STATUS="DOWN"
    PORT_STAT="OFFLINE"
    CPU="0.0"
    MEM="0MB"
    WARN="!! CRITICAL !!"
fi

# 3. 로그 기록 (포트와 경고 내역 포함)
echo "[$TIMESTAMP] Status: $STATUS | Port: $PORT_STAT | CPU: ${CPU:-0.0}% | Mem: ${MEM:-0MB} | Msg: $WARN" >> $LOG_FILE
```
<img width="889" height="487" alt="스크린샷 2026-05-15 오전 12 56 22" src="https://github.com/user-attachments/assets/d071713a-4b9c-4b0a-9b2f-5c2c6760db58" />





## 5. 학습 고찰 및 핵심 개념 정리 (Q&A)

### Q1. SSH 포트 변경과 Root 원격 접속 차단이 왜 기본 보안의 핵심인가?
## 📚 주요 개념 이해하기

서버 보안 설정을 이해하기 위해 반드시 알아야 할 두 가지 핵심 개념입니다.

---

### 1️⃣ SSH (Secure Shell)
> **"안전하게 원격으로 접속하는 비밀 터널"**

* **의미**: **S**ecure(보안) **Sh**ell(명령어 창)의 약자입니다.
* **역할**: 멀리 떨어져 있는 서버에 내 컴퓨터로 접속하여 명령을 내릴 수 있게 해주는 **'통로'**입니다.
* **특징**: 옛날 방식(Telnet 등)과 달리 모든 데이터를 **암호화**하여 주고받습니다. 해커가 중간에 데이터를 가로채도 내용을 알 수 없어 안전합니다.
* **사용처**:
    * 집이나 외부에서 학교/회사 서버에 접속할 때
    * AWS, Google Cloud 등 클라우드 서버를 관리할 때
    * 서버실에 가지 않고 내 노트북으로 서버 설정을 변경할 때

---

### 2️⃣ Root (루트)
> **"모든 것을 할 수 있는 절대 권한자"**

* **의미**: 식물의 **'뿌리'**라는 뜻으로, 리눅스의 모든 파일과 설정이 시작되는 지점을 의미합니다.
* **역할**: 시스템의 **최고 관리자 계정**입니다. 윈도우의 관리자 권한보다 훨씬 강력한 힘을 가집니다.
    * 시스템 전체 삭제 가능
    * 사용자 생성 및 삭제 권한
    * 모든 보안 설정 변경 가능
* **주의점**: 너무 강력해서 위험합니다. 실수로 `rm -rf /` (전체 삭제) 명령을 내리면 서버가 즉시 파괴되므로 주의가 필요합니다.
* **사용처**:
    * 새로운 소프트웨어를 설치할 때
    * 시스템 보안 설정(포트 변경 등)을 바꿀 때
    * 다른 사용자들의 권한을 관리할 때

---

### 3️⃣ 왜 이 둘을 함께 설정해야 하나요?

해커에게 서버 해킹이란 **"SSH라는 통로를 통해 Root라는 절대 권한을 뺏는 것"**을 의미합니다. 이를 막기 위해 이중 잠금 장치를 합니다.

1.  **SSH 포트 변경 (통로 은폐)**: 해커가 우리 집으로 들어오는 **'통로' 자체를 못 찾게** 하여 공격 시도 자체를 줄입니다.
* 모르는 번호의 문(20022)으로 들어와서, 일반인 열쇠로 먼저 확인받아야만 서버 입장이 가능하도록 설계한 것입니다.
* 전 세계의 해커들은 봇(Bot)을 이용해 전형적인 SSH 포트인 **22번**을 대상으로 무차별 대입 공격(Brute-force)을 시도합니다.
* 포트를 **20022**와 같은 비표준 포트로 변경하는 것만으로도 자동화된 공격의 90% 이상을 회피할 수 있습니다(Security by Obscurity).
  
2.  **Root 접속 차단 (열쇠 보호)**:
   설령 해커가 통로를 찾아냈더라도, **'가장 강력한 열쇠(Root)'**는 아예 문 밖으로 가지고 나갈 수 없게 못 박아버리는 것입니다.
* **Root 계정**은 시스템의 전권을 가진 계정이므로, 원격 접속을 허용할 경우 단 한 번의 비밀번호 유출로 시스템 전체가 장악될 위험이 있습니다. 따라서 일반 계정으로 접속 후 필요한 경우에만 `sudo`를 사용하는 것이 표준 보안 절차입니다.

### Q2. "필요 포트만 허용"하는 방화벽 정책의 중요성은 무엇인가?
*   **답변**: 서버에 열려 있는 모든 포트는 잠재적인 공격 통로(Attack Surface)입니다. UFW를 통해 서비스에 꼭 필요한 **20022(SSH)**와 **15034(APP)**만 허용하고 나머지를 차단(Default Deny)함으로써, 알 수 없는 서비스의 취약점을 통한 침입을 원천 봉쇄할 수 있습니다.

### Q3. 계정/그룹과 ACL을 통해 디렉토리를 분리하는 이유는 무엇인가?
*   **답변**: **최소 권한 원칙(Principle of Least Privilege)**을 실현하기 위함입니다. `agent-test` 계정은 테스트 파일에만 접근하고, 보안이 중요한 `api_keys`나 로그 파일에는 접근할 수 없도록 격리함으로써 내부 사용자에 의한 실수나 악의적인 데이터 유출 사고를 방지할 수 있습니다.

### Q4. 환경 변수(AGENT_HOME 등)로 실행 환경을 고정하는 이유는 무엇인가?
*   **답변**: 서비스가 설치된 경로가 사용자마다 다르거나 실행 위치에 따라 상대 경로가 바뀌면 프로그램이 오작동할 수 있습니다. 환경 변수를 사용하면 시스템 내 어디서든 **동일한 환경(Context)**에서 앱이 실행되도록 보장할 수 있으며, 관리 및 이식성이 높아집니다. `echo $AGENT_HOME` 명령어로 즉시 설정값을 검증할 수 있습니다.

### Q5. 쉘 스크립트로 시스템 상태를 수집하고 로그를 남기는 흐름의 장점
*   **답변**: 장애가 발생했을 때 과거의 상태를 알 수 없다면 원인 분석은 불가능해집니다. `monitor.sh`를 통해 주기적으로 CPU, 메모리, 포트 상태를 기록해 두면, 장애 발생 시점의 로그를 분석하여 **"언제, 어떤 자원이 부족해서 문제가 생겼는지"** 객관적으로 추적하고 재발 방지 대책을 세울 수 있습니다.

### Q6. Crontab 주기 실행과 로그 보존 정책(압축/삭제)이 왜 필요한가?
*   **답변**: 모니터링은 끊김 없이 지속되어야 하므로 `crontab`을 통한 자동화가 필수입니다. 하지만 무한정 생성되는 로그는 결국 **디스크 풀(Disk Full)** 장애를 유발하여 서버를 멈추게 만듭니다. 따라서 일정 기간이 지난 로그는 압축하여 용량을 줄이고, 오래된 데이터는 삭제하는 보존 정책이 서버의 가용성을 유지하는 데 핵심적인 역할을 합니다.

